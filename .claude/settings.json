{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push*)",
            "command": "CMD=$(jq -r '.tool_input.command'); case \"$CMD\" in \"git push\"*) cd /Users/prabhune/Documents/ensemble-stage && cargo fmt --all && cargo clippy --all-targets -- -D warnings || { echo '{\"continue\": false, \"stopReason\": \"cargo fmt or clippy failed — fix before pushing\"}'; exit 1; } ;; esac",
            "timeout": 120,
            "statusMessage": "Running cargo fmt and clippy..."
          }
        ]
      }
    ]
  }
}
